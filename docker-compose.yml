services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: magicaibuilder
      POSTGRES_USER: magic
      POSTGRES_PASSWORD: magic
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U magic -d magicaibuilder"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
